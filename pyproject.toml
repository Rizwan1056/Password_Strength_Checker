[project]
name = "password-check"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.43.2",
]

[tool.uv.workspace]
members = ["library-system"]
